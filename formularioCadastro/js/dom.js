function addErrors(input) {         //adiciona estilizacao de erro
    input.classList.add('error-active');
}

function removeErrors(input) {      //remove estilizacao de erro
    input.classList.remove('error-active');
}